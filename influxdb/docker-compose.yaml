version: "3"
services:
  influxdb:
    image: influxdb
    container_name: influxdb
    restart: unless-stopped
    ports:
      - $(INFLUX-PORT):8086
    volumes:
      - /etc/timezone:/etc/timezone
      - $(INFLUX-DIR):/var/lib/influxdb2
      - $(INFLUX-DIR):/etc/influxdb2